# Viddeo.app - Video compression in browser


## Start the project locally

```shell
npm install
npm run dev
```

The dev server is then running under http://localhost:3000.

## Run code quality tools

```shell
npm run lint
npm run prettier
```

## Create translations

```shell
npm run l10n
```

